"use strict";
/*let usuario : {nome:string, idade?:number}
usuario = {nome:"Rubens", idade:45}
console.log(usuario, typeof(usuario))

interface Pessoa {
  nome:string
  sobrenome?:string
  idade:number
}

interface Pessoa{
  nascimento: string
}

interface Funcionario extends Pessoa{
  salario:number
}

class Diretor implements Funcionario{
  salario: 2554
  nome: "Alexandre"
  idade: 40
  nascimento: "01/01/2001"
}
console.log(Diretor, typeof(Diretor))

type Pontos = {
  x:number,
  y:number
}

type Dimensoes = {
  altura:number,
  largura:number
}

type Retangulo = Pontos & Dimensoes

const quadrado:Retangulo = {
  altura:100,
  largura:100,
  x:0,
  y:0
}

console.log(quadrado, typeof(quadrado))*/
